<h1><%= @doctor.name %></h1>

<h3>Department: </h3>
<%= @doctor.department %>
<h3>Appointments:</h3>
<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li><%= appointment.date_time %> for <%= link_to appointment.patient.name, patient_path(appointment.patient) %> </li>

  <% end %>
</ul>
